(() => {
  const A=window.SIR_ACCT;
  A.views.invoices=view=>{
    const s=A.state,invoiced=new Set(s.invoices.filter(i=>i.kind==='sale').map(i=>i.order_id)),ready=s.orders.filter(o=>o.status==='completed'&&!invoiced.has(o.id));
    view.innerHTML=`<section class="panel"><div class="panel-head"><span>Готовы к выставлению</span><span class="mini">Номер присваивается только сервером</span></div><div class="table-wrap"><table class="table"><thead><tr><th>Заказ</th><th>Клиент</th><th>Дата выполнения</th><th>Сумма</th><th></th></tr></thead><tbody>${ready.map(o=>`<tr><td><b>${A.esc(o.order_no)}</b></td><td>${A.esc(o.customer_name)}</td><td>${o.completed_at?A.iso(o.completed_at):'—'}</td><td>${A.money(o.final_price??o.preliminary_price)}</td><td><button class="btn primary issue-invoice" data-order="${o.id}">Выставить счёт</button></td></tr>`).join('')||'<tr><td colspan="5">Нет выполненных заказов без счёта.</td></tr>'}</tbody></table></div></section><section class="panel"><div class="panel-head"><span>Счета и кредит-ноты</span><button class="btn" data-export="invoices">CSV</button></div><div class="table-wrap"><table class="table"><thead><tr><th>№</th><th>Дата</th><th>Клиент</th><th>Тип</th><th>Netto</th><th>MVA</th><th>Итого</th><th>Статус</th><th></th></tr></thead><tbody>${s.invoices.map(i=>`<tr><td><b>${i.invoice_no}</b></td><td>${A.esc(i.invoice_date)}</td><td>${A.esc(i.buyer_name)}</td><td>${i.kind==='credit'?'Кредит-нота':'Счёт'}</td><td>${A.money(i.amount_net)}</td><td>${A.money(i.vat_amount)}</td><td>${A.money(i.amount_gross)}</td><td>${A.esc(i.status)}</td><td><div class="toolbar"><a class="btn tiny" href="invoice.html?id=${encodeURIComponent(i.id)}" target="_blank">Открыть</a>${i.kind==='sale'&&i.status==='issued'?`<button class="btn tiny mark-paid" data-id="${i.id}">Оплачено</button><button class="btn tiny credit-note" data-id="${i.id}">Кредит-нота</button>`:''}</div></td></tr>`).join('')||'<tr><td colspan="9">Счетов пока нет.</td></tr>'}</tbody></table></div></section>`;
    view.querySelectorAll('.issue-invoice').forEach(b=>b.addEventListener('click',()=>issue(b.dataset.order)));
    view.querySelectorAll('.mark-paid').forEach(b=>b.addEventListener('click',()=>paid(b.dataset.id)));
    view.querySelectorAll('.credit-note').forEach(b=>b.addEventListener('click',()=>credit(b.dataset.id)));
    view.querySelector('[data-export="invoices"]').addEventListener('click',()=>A.exportCsv('invoices'));
  };
  async function issue(orderId){const {data,error}=await A.sb.rpc('create_accounting_invoice_from_order',{p_order_id:orderId});if(error){alert(error.message);return;}await A.load();A.render();if(data?.id)window.open(`invoice.html?id=${encodeURIComponent(data.id)}`,'_blank');}
  async function paid(id){const method=prompt('Способ оплаты: bank / card / cash / vipps / other','bank');if(method===null)return;const m=String(method).trim().toLowerCase();if(!Object.keys(A.methods).includes(m)){alert('Допустимо: bank, card, cash, vipps, other');return;}const {error}=await A.sb.rpc('set_accounting_invoice_paid',{p_invoice_id:id,p_paid:true,p_method:m});if(error){alert(error.message);return;}await A.load();A.render();}
  async function credit(id){if(!confirm('Создать новый нумерованный кредит-нот и пометить исходный счёт как credited?'))return;const {data,error}=await A.sb.rpc('create_accounting_credit_note',{p_invoice_id:id});if(error){alert(error.message);return;}await A.load();A.render();if(data?.id)window.open(`invoice.html?id=${encodeURIComponent(data.id)}`,'_blank');}
})();